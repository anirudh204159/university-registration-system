package UniversitySystem.Model.Schedule;

/*
 * Enum for representing the days of the week that classes can be scheduled on.
 * Used in the Schedule entity to avoid hardcoding day strings.
 */
public enum Day {
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY,
    FRIDAY
}
